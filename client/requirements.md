## Packages
framer-motion | Smooth scroll animations and entry effects
react-hook-form | Form state management and validation
zod | Schema validation (already in base, but emphasizing for form)
@hookform/resolvers | Zod resolver for react-hook-form

## Notes
The contact form should POST to /api/messages.
No other dynamic data fetching is required for this static landing page.
Focus on semantic HTML5 structure (header, main, section, footer).
